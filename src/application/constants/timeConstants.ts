/**
 * Time-related constants for the application
 */
export const TIME_CONSTANTS = {
  /**
   * The expiry time for forgot password tokens in milliseconds
   * 20 minutes = 20 * 60 * 1000 = 1200000 milliseconds
   */
  FORGOT_PASSWORD_TOKEN_EXPIRY: 20 * 60 * 1000,
};
